name: AFW-BE-REQUEST-RECONCILIATION-3

on:
  push:
    branches:
      - feature/backend-request-reconciliation-3
    paths:
      - 'backend/src/FinancialPlatform/Reconciliation/**'
      - 'backend/tests/Reconciliation.CandidateQuery.Scenarios/**'
      - 'backend/tests/Reconciliation.CandidateMatching.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewQueue.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewOrchestration.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewHttp.Scenarios/**'
      - '.github/workflows/backend-request-reconciliation-3.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/src/FinancialPlatform/Reconciliation/**'
      - 'backend/tests/Reconciliation.CandidateQuery.Scenarios/**'
      - 'backend/tests/Reconciliation.CandidateMatching.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewQueue.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewOrchestration.Scenarios/**'
      - 'backend/tests/Reconciliation.ReviewHttp.Scenarios/**'
      - '.github/workflows/backend-request-reconciliation-3.yml'
  workflow_dispatch:

jobs:
  validate:
    name: Validate protected reconciliation review HTTP API
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Build Reconciliation Application
        run: dotnet build backend/src/FinancialPlatform/Reconciliation/Reconciliation.Application/Reconciliation.Application.csproj --configuration Release --nologo

      - name: Build Reconciliation Infrastructure
        run: dotnet build backend/src/FinancialPlatform/Reconciliation/Reconciliation.Infrastructure/Reconciliation.Infrastructure.csproj --configuration Release --nologo

      - name: Build Reconciliation API
        run: dotnet build backend/src/FinancialPlatform/Reconciliation/Reconciliation.Api/Reconciliation.Api.csproj --configuration Release --nologo

      - name: Run automatic reconciliation candidate query scenarios
        run: dotnet run --project backend/tests/Reconciliation.CandidateQuery.Scenarios/Reconciliation.CandidateQuery.Scenarios.csproj --configuration Release

      - name: Run automatic reconciliation candidate matching scenarios
        run: dotnet run --project backend/tests/Reconciliation.CandidateMatching.Scenarios/Reconciliation.CandidateMatching.Scenarios.csproj --configuration Release

      - name: Run reconciliation review queue and decision scenarios
        run: dotnet run --project backend/tests/Reconciliation.ReviewQueue.Scenarios/Reconciliation.ReviewQueue.Scenarios.csproj --configuration Release

      - name: Run reconciliation review store and orchestration scenarios
        run: dotnet run --project backend/tests/Reconciliation.ReviewOrchestration.Scenarios/Reconciliation.ReviewOrchestration.Scenarios.csproj --configuration Release

      - name: Run protected reconciliation review HTTP scenarios
        run: dotnet run --project backend/tests/Reconciliation.ReviewHttp.Scenarios/Reconciliation.ReviewHttp.Scenarios.csproj --configuration Release
