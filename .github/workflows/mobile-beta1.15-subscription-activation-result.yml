name: Mobile Beta1.15 Subscription Activation Result

on:
  push:
    branches:
      - feature/mobile-beta1.15
    paths:
      - 'apps/mobile_app/**'
      - '.github/workflows/mobile-beta1.15-subscription-activation-result.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'apps/mobile_app/**'
      - '.github/workflows/mobile-beta1.15-subscription-activation-result.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile_app

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test subscription activation result
        run: flutter test test/subscription_activation_result_test.dart

      - name: Test subscriptions navigation
        run: flutter test test/subscriptions_navigation_test.dart

      - name: Run complete Flutter test suite
        run: flutter test

      - name: Verify commit diff
        working-directory: .
        run: git diff --check HEAD^ HEAD
