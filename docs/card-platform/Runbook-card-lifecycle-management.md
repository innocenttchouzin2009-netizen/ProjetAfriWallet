# Runbook — Card Lifecycle Management

## Start the API
Run the CardPlatform API and use the lifecycle endpoints.

## Validate behavior
Execute the lifecycle scenario project to verify transitions and audit output.
