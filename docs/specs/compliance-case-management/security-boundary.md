# Security Boundary

The case stores AWID, title, source references and summaries, assignments, notes, decisions, and audit metadata. It stores no raw provider datasets, screening implementation, AML rules, scoring implementation, regulator credentials, or filing secrets.