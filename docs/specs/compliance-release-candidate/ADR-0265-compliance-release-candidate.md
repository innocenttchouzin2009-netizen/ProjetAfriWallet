# ADR-0265 - Compliance Platform Release Candidate

Sprint 16 is packaged as `v1.6.0-rc1`. Tags `sprint16-dlv-0016.1` through `sprint16-dlv-0016.7` must exist locally/remotely, resolve identically, and belong to main history.

The final immutable tag is `compliance-platform-v1.6.0-rc1` and may be created only after the RC PR squash merge. Existing delivery tags are never moved.