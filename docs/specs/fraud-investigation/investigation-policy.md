# Investigation Policy - Sandbox

Cases require a matching fraud-decision snapshot for the same transaction and AWID before creation.

Closed cases are immutable. Resolution is permitted only from `UNDER_INVESTIGATION` or `ESCALATED`.