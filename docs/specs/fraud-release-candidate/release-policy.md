# Release Policy

The RC package requires 7/7 frozen deliveries, 18/18 RC checks, zero failed checks, zero skipped checks, a valid SHA-256 manifest, and the decision `READY FOR FRAUD RC`.

The final tag is intentionally absent until the RC PR has been squash-merged and its exact merge SHA has been verified in `origin/main`.