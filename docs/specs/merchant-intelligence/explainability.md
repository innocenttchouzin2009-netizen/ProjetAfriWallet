# Merchant Intelligence Explainability

Every finding contains score, severity, recommendation, metrics and explicit patterns. Each pattern includes a stable code, score contribution, human-readable reason and source references. The engine cannot emit an unexplained non-zero score and uses no opaque machine-learning classifier.
