# Artifacts

This release candidate is source-verified: no compiled binaries are checked into the repository. Build artifacts are produced on demand via `dotnet build`/`dotnet publish` from the tagged commit `dispute-platform-v1.8.0-rc1`.
