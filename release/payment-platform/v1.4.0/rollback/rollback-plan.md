# Payment Platform v1.4.0 Rollback

Rollback deploys the last approved immutable artifact digest and disables affected
provider routes through controlled configuration. Preserve correlation, audit,
telemetry, and reconciliation evidence before rollback.

Historical delivery tags are immutable and must never be moved to represent a
rollback.